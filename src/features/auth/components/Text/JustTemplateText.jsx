/** @format */

import Text from 'src/components/Text';

const JustTemplateText = () => {
	return (
		<Text
			tag={'p'}
			formatTag='i'
			text={
				'* This is just a template. No valid data required during registration.'
			}
		/>
	);
};

export default JustTemplateText;
