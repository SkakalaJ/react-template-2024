/** @format */

import Text from 'src/components/Text';

const OrSignInWithText = () => {
	return (
		<Text
			tag={'p'}
			formatTag='b'
			text={'or sign in with'}
		/>
	);
};

export default OrSignInWithText;
