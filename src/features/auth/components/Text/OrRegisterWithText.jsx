/** @format */

import Text from 'src/components/Text';

const OrRegisterWithText = () => {
	return (
		<Text
			tag={'p'}
			formatTag='b'
			text={'or register with'}
		/>
	);
};

export default OrRegisterWithText;
